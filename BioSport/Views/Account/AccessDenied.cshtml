@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5 text-center">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="text-danger mb-3">Acceso Denegado</h1>
            <p class="fs-5 text-muted">
                No tienes permisos para ver esta página. Si crees que esto es un error,
                contacta a un administrador.
            </p>
            <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary mt-3">Volver al Panel</a>
        </div>
    </div>
</div>
